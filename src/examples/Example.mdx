import { Canvas, Meta } from '@storybook/blocks';
import * as Example from './Example.stories';
 
<Meta of={Example} />
 
# Example
 
This is a basic template for an mdx file that uses the `Canvas` component to render a story.
 
<Canvas of={Example.Primary} />